export default class BasicSuperMarketEntity {
  constructor({ address, location, employees, systemPassword }) {
    this.address = address;
    this.location = location;
    this.employees = employees;
    this.systemPassword = systemPassword;
  }
}
