export default class SuperMarketDto {
  constructor({ address, location, employees, open }) {
    this.address = address;
    this.location = location;
    this.employees = employees;
    this.open = open;
  }
}
